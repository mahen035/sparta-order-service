spring.application.name=order-service
server.port=${SERVER_PORT:8080}

product-service.base-url=${PRODUCT_SERVICE_URL:http://localhost:8081}

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/order_service}
spring.datasource.username=${DB_USERNAME:order_service}
spring.datasource.password=${DB_PASSWORD:order_service}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
